# Task Completion
- For static-site edits, verify the affected HTML/CSS/JS in a browser.
- If UI behavior changed, prefer a quick browser validation with Playwright or manual browser testing.
- There is no repo-local lint/test pipeline detected, so completion is usually visual/manual verification plus checking for obvious console errors.
- Preserve existing manual coding style and avoid introducing a build step unless explicitly requested.